# WorkBuddy 记忆 — 指针

本目录**不存放正文**。项目记忆的唯一事实来源在：

- `docs/agent-memory/decisions.md` —— 决策与用户偏好
- `docs/agent-memory/pitfalls.md` —— 坑与「不要做什么」（硬约束）
- `docs/agent-memory/log/YYYY-MM-DD.md` —— 会话流水，只追加
- `docs/agent-memory/README.md` —— 机制说明与写入规范
- `AGENTS.md` —— 面向人的总纲

开工前先读上面几个文件；本次会话若有非显然的新结论，按 README 的规范追加到
`docs/agent-memory/`，以 `memory:` 前缀单独提交，这样其他 agent 才能接续。
