# PG4 Smart Assist — 前情提要 SPEC

> 本文档为整份 SPEC 的前情提要，仅记录**已确认的事实、决策与定稿内容**，作为后续所有实现层 SPEC 的唯一锚点。不涉及技术选型、框架选择或代码结构。

---

## 一、原始诉求

pgAdmin4 是 PostgreSQL 官方推荐的 Web 端管理工具，但其 SQL 编辑器的智能辅助能力严重滞后于现代 IDE（DataGrip、VS Code + SQLTools）。开发者在"写 SQL"这一核心动作上遭遇大量摩擦，被迫在编辑器与文档/记忆之间反复切换，严重打断心流。

**本插件定位**：pgAdmin4 网页版之上的浏览器增强层，不修改后端、不替换编辑器，以"寄生式增强"的方式补齐体验短板。

---

## 二、用户关键决策（已确认）

| 决策点 | 结论 |
|--------|------|
| **支持版本** | pgAdmin4 **v8.4 及以上**（当前最新 v9.14）。v8.4 起编辑器统一使用 CodeMirror 6，无需兼容 CodeMirror 5。放弃 v8.3 及以下版本。 |
| **CodeMirror 交互边界** | **放宽技术实现限制**。允许通过任何可行方式（包括获取编辑器实例）来实现上下文感知与实时校验。本 SPEC 不对此做技术方向性约束。 |
| **离线 Schema 缓存构建** | 用户手动导出 `pg_dump --schema-only` 文件并上传至插件，插件解析建表语句建立本地索引。本 SPEC 仅定义此数据流方向，不约束具体解析/存储技术。 |
| **JSONB 键路径数据来源** | 用户在 DDL 导出文件中**额外定义 JSONB 内部 Field 结构**，插件据此构建 JSONB 键路径补全索引。不依赖运行时结果集解析。 |

---

## 三、现存问题清单（定稿）

### 3.1 自动补全

| 编号 | 问题描述 |
|:---:|:---|
| A-1 | 补全依赖实时查询系统表，延迟高（数百毫秒至数秒），极端情况下可能阻塞 Query Tool |
| A-2 | SQL 上下文感知严重不足（有基础识别但粒度极粗），FROM 后仍弹列名，SELECT 后仍混杂表名/函数名，候选噪音大 |
| A-3 | 排序固定为字母序/系统表返回序，无频次、无最近使用权重 |
| A-4 | 不支持 JSONB 内部键路径补全（`->>` 后无候选） |
| A-5 | 对表别名、CTE 支持不完善（曾引入部分 pgcli 逻辑但覆盖不全），复杂嵌套/多表 JOIN 场景识别失败 |

### 3.2 编辑体验

| 编号 | 问题描述 |
|:---:|:---|
| B-1 | 粘贴字段名至 WHERE/VALUES 时不会自动包裹单引号，需手动添加 |
| B-2 | JSONB 列在结果集中展示为格式化文本，无法快速提取子路径的值 |
| B-3 | 无实时语法校验，错误需执行后才暴露 |
| B-4 | 无类型不匹配预警（如 varchar 与 int 比较） |

### 3.3 信息获取

| 编号 | 问题描述 |
|:---:|:---|
| C-1 | 查看注释需展开左侧对象树逐层点击，无法在编辑区即开即得 |
| C-2 | 悬停信息维度不足（仅显示注释，缺类型/默认值/可空/外键） |
| C-3 | 历史 DDL 版本对比缺失（原生 Schema Diff 仅支持实时库对比，不支持历史快照对比） |

### 3.4 效率与安全

| 编号 | 问题描述 |
|:---:|:---|
| D-1 | SQL 片段管理能力弱（仅有基础 Macro，无分类/占位符/快捷展开） |
| D-2 | 查询历史持久化容量受限（默认仅 20 条，检索能力弱） |
| D-3 | 原生危险操作拦截简陋（无影响行数预估，无法识别 `WHERE 1=1` 等绕过场景） |

---

## 四、期望达成的效果（按优先级）

### P0 — 核心体验：补全必须"快、准、懂我"

1. **即时响应**：补全延迟 < 50ms（P95），不依赖实时数据库查询
2. **深度上下文感知**：根据 SELECT / FROM / WHERE / JOIN / ORDER BY / 别名前缀 / 函数参数 自动过滤候选类型
3. **智能排序**：融合频次、最近使用、局部热度、主键/外键权重
4. **JSONB 深度补全**：输入 `->>` 或 `#>>` 时弹出该列所有已知 JSON 键路径（含嵌套）
5. **别名 & CTE 感知**：输入 `alias.` 时仅展示该表字段

### P1 — 编辑增强：减少机械操作，前置反馈

6. **智能粘贴包裹**：根据上下文自动添加单引号/双引号
7. **JSONB 区域精准复制**：剥离格式化缩进，仅保留选中原始值
8. **实时语法与类型校验**：红色波浪线标记语法错误，黄色波浪线标记类型不匹配
9. **增强悬停文档**：浮层展示类型、默认值、是否可空、注释、外键指向

### P2 — 信息可视化

10. **JSONB 结构树预览**：树形展示 JSON 键路径，点击节点生成 `->>'完整路径'` 并插入编辑器
11. **历史 DDL 快照对比**：保存多版本 DDL 快照，支持任意两版本 Diff 高亮

### P3 — 效率与安全

12. **增强型 SQL 片段库**：分类、变量占位符、一键展开
13. **查询历史增强检索**：全量持久化，支持时间/数据库/关键词检索
14. **智能危险操作拦截**：无 WHERE 的 UPDATE/DELETE、TRUNCATE、DROP 时弹出二次确认，并展示影响行数预估。影响行数预估通过 pgAdmin4 **已有的查询通道**执行 EXPLAIN 获取，插件自身不建立独立数据库连接。

---

## 五、核心设计原则

| 原则 | 说明 |
|------|------|
| 零后端侵入 | 不修改 pgAdmin4 后端，不依赖额外服务部署 |
| 离线优先 | Schema 信息通过 DDL 文件构建本地缓存，补全不依赖实时查询 |
| 渐进增强 | 插件失效时 pgAdmin4 原生功能不受影响 |
| 隐私本地化 | 所有数据（查询历史、频次、片段、DDL 快照）仅存浏览器本地，不上传 |
| 多环境切换 | 支持多套 Schema 快照，一键切换活跃版本 |

---

## 六、边界与不做的事

- ❌ 不修改 pgAdmin4 源码或后端逻辑
- ❌ 不替代 pgAdmin4 的查询执行、结果渲染、连接管理
- ❌ 不做 SQL 自动改写/性能优化建议
- ❌ 不做多人协作/云端同步

**允许的交互方式**：允许通过任何只读方式（包括但不限于获取编辑器实例、观察 DOM、监听事件）读取 pgAdmin4 当前状态，但**不得覆盖或替换**原生功能的行为。

---

## 七、成功衡量标准

| 指标 | 目标 |
|------|------|
| 补全触发到候选渲染 | ≤ 50ms（P95） |
| 上下文判断准确率 | ≥ 95% |
| 输入 → 选中候选的平均击键数 | ≤ 3 次（原生约 6 次） |
| 语法错误发现时机 | 输入中 < 1s 内标记 |
| 切换窗口/查文档次数 | 减少 ≥ 70% |

---

## 八、下一阶段输入物

以下内容将交由下一阶段的实现层 SPEC（Vibe Coding SPEC）承接：

- 插件与 pgAdmin4 CodeMirror 6 编辑器的交互边界与事件策略
- Schema 图谱的数据结构设计与存储方案
- 上下文解析器精度与性能平衡
- 排序算法权重配比与冷启动策略
- 与 pgAdmin4 v8.4 ~ v9.x 的兼容性方案
- 浏览器兼容性矩阵（Chrome / Edge / Firefox）
- DDL 解析器对 JSONB Field 定义的支持

---

> 📌 本前情提要 SPEC 为**已定稿内容**，后续所有实现层 SPEC 必须以此为依据，不得擅自修改本文档中已确认的事实、决策与效果定义。如有歧义，以本文档为准。
>
> 请生成一个准备开发的详细SPED.md，如果有任何不确定的细节请随时问我。