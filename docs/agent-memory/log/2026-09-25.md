# 2026-09-25

## 工作树状态（接续开发前先看这里）

- `HEAD` = `0d44877` v2.2.4（`main` 已同步 `origin` / `github`）。
- 工作树有 **4 个未提交改动**：`AGENTS.md`、`README.md`、`pg4-assist.js`、
  `docs/pgAdmin 4 Web 技术特征与验证知识库.md`。
- 工作树里的关键常量：`VERSION = "2.2.7"`、`CONFIG_VERSION = 2`、`GRID_HOOK_REV = 9`
  （注意：`VERSION` 已高于最近提交的 v2.2.4，属于「连续小修不逐次升版本」之外的积压版本号，
  提交时留意别把它当成本次会话的改动）。

## 实测数据

- 本机私有的全量 DDL 快照导入成功：**358 relations / 5714 columns / 80 functions，0 warning**。
  导入方式：浏览器 file input + `window.__pg4.importDdlText`；持久化在 IndexedDB。
- 导入前顶层页面 `window.__pg4Assist.graph` 为 `null`（属预期，导入后才非空）。

## 本次会话：建立跨 agent 记忆机制

- 背景：`.trae/specs/`、`.workbuddy/memory/` 均为空壳；各平台记忆互不可见。
- 决策：新增 `docs/agent-memory/` 作为唯一事实来源，各平台目录只留指针 stub。
- 已新增：`docs/agent-memory/{README,decisions,pitfalls}.md`、`CLAUDE.md`、
  `.github/copilot-instructions.md`、`.cursor/rules/agent-memory.mdc`、
  `.trae/rules/project_rules.md`、`.workbuddy/memory/README.md`，并在 `AGENTS.md` 加「项目记忆」一节。
